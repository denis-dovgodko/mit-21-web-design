@model List<Hall>
@{
}
<div class="Index">
        <h3>Наші зали: </h3>
            @{
                foreach (var b in Model)
                {
                     <a asp-controller="Read" asp-action='Sessions' asp-route-hallId="@b.Id">@b.HallName</a>
                     <br> 
                }
            }
</div>

