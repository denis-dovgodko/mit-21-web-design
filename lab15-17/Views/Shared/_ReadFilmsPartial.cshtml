@model IEnumerable<Film>
@{
}
<div class="Index">
    <h3>Популярні фільми: </h3>
        @foreach (var b in Model)
        {
            <a asp-controller="Read" asp-action='Sessions' asp-route-filmId="@b.Id">@b.Name</a>
             <br>
        }
</div>
